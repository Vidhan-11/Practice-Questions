#include<stdio.h>
int main(){
    int number,total = 1,i = 10;
    printf("Input number :");
    scanf("%d",&number);
    while(number > 0){
        total *= number%i;
        i*= 100;
    }
    printf("Total : %d",total);
    return 0;
}
